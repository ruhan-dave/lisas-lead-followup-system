import { defineConfig } from "@trigger.dev/sdk/v3";

export default defineConfig({
  // Project configuration
  project: "proj_zngcaxgwflvcxympxixg",
  
  // Max duration for tasks (required, minimum 5 seconds)
  maxDuration: 300, // 5 minutes
});
